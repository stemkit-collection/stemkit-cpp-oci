# vim: ft=jam: sw=2:

local library = sk.oci ;

local objects = 
  Exception.cc
;

make-library $(library) : $(objects) ;

add-project-library $(library) : 
  $(library).db 
  $(library).info
  $(library).abstract
;

make-subdirs info abstract db mock ;

