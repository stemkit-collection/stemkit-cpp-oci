# vim: ft=jam: sw=2:

local library = sk.oci.db.bind ;

local objects = 
  Registry.cc
  Data.cc
  IntData.cc
  StringData.cc
;

use-oracle $(objects) $(library). ;
make-library $(library) : $(objects) ;
